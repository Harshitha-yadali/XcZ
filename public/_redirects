# Netlify / Cloudflare Pages SPA fallback.
# Static hosts already serve an exact file match (including prerendered
# <route>/index.html pages generated by scripts/prerender.mjs) before falling
# back to a rewrite rule, so this only kicks in for routes with no matching
# prerendered file — client-side React Router then takes over.
/*    /index.html   200
